mapscripts SafariZone_Northeast_MapScripts {
    MAP_SCRIPT_ON_LOAD {
        special(SetRoofBirds)
        special(SetStationaryWilds)
    }
}

// Event scripts for SafariZone_Northeast are in SafariZone_South/scripts.inc

